export class Book {
  public title: string;
  public author: string;
  public rating: number;

  constructor(title: string, author: string, rating: number) {
    this.title = title;
    this.author = author;
    this.rating = rating;
  }
}
